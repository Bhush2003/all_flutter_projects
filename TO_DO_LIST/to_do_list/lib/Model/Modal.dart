import 'package:flutter/material.dart';

class ModalOnbording {
  Widget image;
  Widget headText;
  Widget subText;
  ModalOnbording({required this.image,required this.headText,required this.subText});
}
